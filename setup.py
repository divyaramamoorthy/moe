from setuptools import setup, find_packages

setup(
    name="moe", 
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # TODO: List your project dependencies here, for example:
        # "torch",
        # "numpy",
    ],
)